/**\n * API Service - Handles all communication with backend\n */\n\nconst API_BASE_URL = window.location.origin + '/api';\n\nclass APIService {\n    constructor() {\n        this.baseURL = API_BASE_URL;\n        this.token = localStorage.getItem('authToken') || null;\n    }\n\n    async login(username, password) {\n        const response = await this._post('/auth/login', {\n            username,\n            password,\n        });\n        return response;\n    }\n\n    async register(username, email, password) {\n        const response = await this._post('/auth/register', {\n            username,\n            email,\n            password,\n        });\n        return response;\n    }\n\n    async logout() {\n        return this._post('/auth/logout', {});\n    }\n\n    setToken(token) {\n        this.token = token;\n        localStorage.setItem('authToken', token);\n    }\n\n    clearToken() {\n        this.token = null;\n        localStorage.removeItem('authToken');\n    }\n\n    get isAuthenticated() {\n        return !!this.token;\n    }\n\n    async getCurrentUser() {\n        return this._get('/users/me');\n    }\n\n    async getGames() {\n        return this._get('/games');\n    }\n\n    async createGameInstance(gameId) {\n        return this._post('/instances', {\n            game_id: gameId,\n        });\n    }\n\n    async stopGameInstance(instanceId) {\n        return this._post(`/instances/${instanceId}/stop`, {});\n    }\n\n    async getStreamingOffer(instanceId) {\n        return this._post(`/streaming/${instanceId}/offer`, {});\n    }\n\n    async setStreamingAnswer(instanceId, answer) {\n        return this._post(`/streaming/${instanceId}/answer`, {\n            answer,\n        });\n    }\n\n    async getStreamingStats(instanceId) {\n        return this._get(`/streaming/${instanceId}/stats`);\n    }\n\n    async sendKeyboardInput(instanceId, key, pressed) {\n        return this._post(`/input/${instanceId}/keyboard`, {\n            key,\n            pressed,\n        });\n    }\n\n    async sendMouseInput(instanceId, x, y, button) {\n        return this._post(`/input/${instanceId}/mouse`, {\n            x,\n            y,\n            button,\n        });\n    }\n\n    async sendInput(instanceId, data) {\n        return this._post(`/input/${instanceId}`, data);\n    }\n\n    async sendChatMessage(instanceId, message) {\n        return this._post(`/chat/${instanceId}`, {\n            message,\n        });\n    }\n\n    async _get(endpoint) {\n        return this._request('GET', endpoint);\n    }\n\n    async _post(endpoint, data) {\n        return this._request('POST', endpoint, data);\n    }\n\n    async _request(method, endpoint, data = null) {\n        const url = `${this.baseURL}${endpoint}`;\n        const options = {\n            method,\n            headers: {\n                'Content-Type': 'application/json',\n            },\n        };\n\n        if (this.token) {\n            options.headers['Authorization'] = `Bearer ${this.token}`;\n        }\n\n        if (data) {\n            options.body = JSON.stringify(data);\n        }\n\n        try {\n            const response = await fetch(url, options);\n\n            if (!response.ok) {\n                const error = await response.json().catch(() => ({}));\n                throw new Error(error.detail || `HTTP ${response.status}`);\n            }\n\n            return await response.json();\n        } catch (error) {\n            console.error(`API Error [${method} ${endpoint}]:`, error);\n            throw error;\n        }\n    }\n}\n\nexport const api = new APIService();
